//bytecode interpreter

use crate::parser;
use crate::parser::lexer;
use crate::std_lib;
use crate::bcc::Bytecode;

use std::collections::HashSet;
use std::collections::HashMap;


pub fn do_the_things(things: Vec<Vec<Bytecode<'input>>>) {
    let stack = Vec::new();
    
}
